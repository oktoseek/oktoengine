# oktoengine
OktoEngine is the core engine behind OktoSeek. It allows users to create, train, validate, and export AI models using a declarative language called OktoScript. Includes automated environment diagnostics (okto doctor) and self-updating via GitHub Releases (okto upgrade).
